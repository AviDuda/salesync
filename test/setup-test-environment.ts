import { installGlobals } from "@remix-run/node";
// eslint-disable-next-line import/no-extraneous-dependencies -- testing is in dev only
import "@testing-library/jest-dom/extend-expect";

installGlobals();
